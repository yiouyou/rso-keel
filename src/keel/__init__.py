"""
rso-keel: governance / provenance / orchestration mid-layer.

Zero platform dependencies — this package must never import app.*, backend.*, or
any RSO-platform-specific code. The dependency arrow is strictly RSO → keel.
"""

__version__ = "0.1.0"
